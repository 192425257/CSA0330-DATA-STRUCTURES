#include <stdio.h>
int main() {
    int n, a[10][10], visited[10]={0}, queue[10], f=0, r=0, i, j;
    scanf("%d", &n);
    for (i=0;i<n;i++)
        for (j=0;j<n;j++)
            scanf("%d",&a[i][j]);
    queue[r++] = 0;
    visited[0] = 1;
    while (f < r) {
        int v = queue[f++];
        printf("%d ", v);
        for (i=0;i<n;i++)
            if (a[v][i] && !visited[i]) {
                queue[r++] = i;
                visited[i] = 1;
            }
    }
    return 0;
}
